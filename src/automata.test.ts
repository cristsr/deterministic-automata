import { Automata } from './automata';
import { State } from './state';

describe('Automata', () => {
  let initialState: State;

  beforeAll(() => {
    return 'hello';
  });

  it('kkeeyy', () => {
    expect('kkeeyy').toBeTruthy();
  });

  it('kkeeyy2', () => {
    expect('kkeeyy').toBeTruthy();
  });

  it('kkeeyyasda', () => {
    expect('kkeeyy').toBeTruthy();
  });
});
